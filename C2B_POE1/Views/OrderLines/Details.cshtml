@model C2B_POE1.Models.OrderLine

@{
    ViewData["Title"] = "Order Line Details";
}

<h1>Order Line Details</h1>

<div>
    <h4>Order Line</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Quantity)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Quantity)</dd>

        <dt class="col-sm-2">Order</dt>
        <dd class="col-sm-10">@Model.PartitionKey</dd>

        <dt class="col-sm-2">Product</dt>
        <dd class="col-sm-10">@Model.ProductRowKey</dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-partitionKey="@Model.PartitionKey" asp-route-rowKey="@Model.RowKey">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
